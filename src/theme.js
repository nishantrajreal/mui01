import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
          main: '#2196f3', // Blue
        },
        secondary: {
          main: '#f44336', // Red
          light: '#ff9800', // Orange
        },
        otherColor: {
          main: '#ffeb3b', // Yellow
        },
      },
})
